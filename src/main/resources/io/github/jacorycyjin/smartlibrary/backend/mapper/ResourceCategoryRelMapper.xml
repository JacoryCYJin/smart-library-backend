<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.jacorycyjin.smartlibrary.backend.mapper.ResourceCategoryRelMapper">

    <!-- 批量查询资源的分类关联信息 -->
    <select id="selectByResourceIds" resultType="map">
        SELECT 
            resource_id AS resourceId,
            category_id AS categoryId
        FROM resource_category_rel
        WHERE resource_id IN
        <foreach collection="resourceIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
